<!DOCTYPE html>
{# Blablabla #}
<html>
  <head>
    <title>Accueil {{ inventory_hostname }}</title>
    <meta charset="utf-8">
  </head>
  <body>
    <h4>Accueil du site {{ inventory_hostname }}</h4>
    Stagiaires:<br>
    <ul>
    {% for stagiaire in stagiaires %}
       <li>{{ stagiaire }}</li>
    {% endfor %}
    </ul>
  </body>
</html>
